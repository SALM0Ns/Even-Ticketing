<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>My Tickets - CursedTicket</title>

  <!-- ✅ Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
  <!-- Google Fonts -->
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  <!-- QRCode.js -->
  <script src="https://cdn.jsdelivr.net/npm/qrcodejs@1.0.0/qrcode.min.js"></script>

  <style>
    body {
      background: linear-gradient(135deg, #0e0e10 0%, #1a1a1d 50%, #2c2c2f 100%);
      color: #f3f3f3;
      font-family: 'Poppins', sans-serif;
      padding: 40px 0;
      min-height: 100vh;
    }

    .page-title {
      background: linear-gradient(45deg, #ff6b6b, #4ecdc4, #45b7d1);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      font-weight: 700;
      text-shadow: 0 0 30px rgba(255, 107, 107, 0.3);
    }

    .ticket-card {
      background: rgba(26, 26, 29, 0.9);
      backdrop-filter: blur(20px);
      border: 1px solid rgba(255, 255, 255, 0.1);
      border-radius: 20px;
      overflow: hidden;
      transition: all 0.4s ease;
      position: relative;
      box-shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
    }

    .ticket-card:hover {
      transform: translateY(-10px) scale(1.02);
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.4);
    }

    .ticket-poster {
      width: 100%;
      height: 220px;
      object-fit: cover;
      transition: transform 0.3s ease;
    }

    .ticket-card:hover .ticket-poster {
      transform: scale(1.05);
    }

    .ticket-details {
      padding: 20px;
    }

    .ticket-title {
      font-weight: 600;
      font-size: 1.25rem;
      margin-bottom: 15px;
    }

    .ticket-info {
      color: #b8b8b8;
      font-size: 0.9rem;
      margin-bottom: 8px;
      display: flex;
      align-items: center;
    }

    .ticket-info i {
      color: #4ecdc4;
      margin-right: 8px;
    }

    .status-badge {
      display: inline-block;
      padding: 8px 16px;
      border-radius: 25px;
      font-size: 0.8rem;
      font-weight: 600;
      text-transform: uppercase;
      margin-top: 15px;
      color: white;
    }

    .status-active {
      background: linear-gradient(45deg, #00b894, #00cec9);
      box-shadow: 0 4px 15px rgba(0, 184, 148, 0.3);
    }

    .status-expired {
      background: linear-gradient(45deg, #d63031, #e17055);
      box-shadow: 0 4px 15px rgba(214, 48, 49, 0.3);
    }

    .qr-section {
      background: rgba(255, 255, 255, 0.05);
      border-radius: 15px;
      padding: 20px;
      text-align: center;
      margin-top: 20px;
    }

    .qr-container {
      width: 144px;
      height: 144px;
      margin: 0 auto 10px;
      position: relative;
      display: flex;
      justify-content: center;
      align-items: center;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 12px;
      overflow: hidden;
    }

    .qr-container canvas {
      position: absolute !important;
      top: 50% !important;
      left: 50% !important;
      transform: translate(-50%, -50%) rotate(0deg) !important;
      animation: none !important;
      transition: none !important;
      display: block !important;
      width: 120px !important;
      height: 120px !important;
      z-index: 2 !important;
    }

    .verification-text {
      color: #888;
      font-size: 0.8rem;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    .no-tickets {
      text-align: center;
      padding: 80px 20px;
      background: rgba(255, 255, 255, 0.05);
      border-radius: 20px;
      border: 2px dashed rgba(255, 255, 255, 0.1);
    }

    .btn-primary {
      background: linear-gradient(45deg, #ff6b6b, #4ecdc4);
      border: none;
      border-radius: 25px;
      padding: 12px 30px;
      font-weight: 600;
      text-transform: uppercase;
      transition: all 0.3s ease;
    }

    .btn-primary:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(255, 107, 107, 0.3);
    }

  </style>
</head>

<body>
  <div class="container">
    <h1 class="text-center mb-5 page-title">🎟️ My Tickets</h1>

    <% if (tickets && tickets.length > 0) { %>
      <div class="row g-4">
        <% tickets.forEach(ticket => { %>
          <div class="col-lg-4 col-md-6">
            <div class="ticket-card">
              <img src="<%= ticket.imageUrl || '/images/default-event.jpg' %>" alt="poster" class="ticket-poster">
              
              <div class="ticket-details">
                <h4 class="ticket-title"><%= ticket.eventName %></h4>
                <div class="ticket-info"><i class="bi bi-calendar-event"></i> 
                  <span><%= new Date(ticket.date).toLocaleDateString('en-US', {year:'numeric',month:'long',day:'numeric'}) %></span>
                </div>
                <div class="ticket-info"><i class="bi bi-geo-alt"></i> <span><%= ticket.location %></span></div>
                <div class="ticket-info"><i class="bi bi-person-badge"></i> <span><strong>Seat:</strong> <%= ticket.seatNumber || 'General' %></span></div>
                <div class="ticket-info"><i class="bi bi-currency-dollar"></i> <span><strong>Price:</strong> $<%= ticket.price %></span></div>
                <span class="status-badge <%= ticket.status === 'Active' ? 'status-active' : 'status-expired' %>">
                  <%= ticket.status %>
                </span>
              </div>

              <div class="qr-section">
                <div class="qr-container" id="qrcode-<%= ticket._id %>"></div>
                <div class="verification-text"><i class="bi bi-qr-code"></i> Verification Code</div>
              </div>
            </div>
          </div>
        <% }); %>
      </div>
    <% } else { %>
      <div class="no-tickets">
        <i class="bi bi-ticket-perforated" style="font-size:4rem;color:#4ecdc4;"></i>
        <h3>No tickets found</h3>
        <p>You haven't purchased any tickets yet.</p>
        <a href="/events" class="btn btn-primary"><i class="bi bi-search me-2"></i>Browse Events</a>
      </div>
    <% } %>
  </div>

  <!-- ✅ Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>

  <!-- ✅ QR Code Script -->
  <script>
    document.addEventListener("DOMContentLoaded", () => {
      document.querySelectorAll('[id^="qrcode-"]').forEach(el => {
        const id = el.id.replace('qrcode-', '');
        const qrData = 'TICKET-' + id + '-' + Date.now();

        // สร้าง QR Code
        const qr = new QRCode(el, {
          text: qrData,
          width: 120,
          height: 120,
          colorDark: "#000000",
          colorLight: "#ffffff",
          correctLevel: QRCode.CorrectLevel.H
        });

        // ปรับตำแหน่งหลัง render
        setTimeout(() => {
          const canvas = el.querySelector("canvas");
          if (canvas) {
            canvas.style.position = "absolute";
            canvas.style.top = "50%";
            canvas.style.left = "50%";
            canvas.style.transform = "translate(-50%, -50%) rotate(0deg)";
            canvas.style.animation = "none";
            canvas.style.transition = "none";
            canvas.style.display = "block";
            canvas.style.width = "120px";
            canvas.style.height = "120px";
          }
        }, 300);
      });
    });
  </script>
</body>
</html>
